<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eastteam.myprogram.dao.SurveyMybatisDao">
    
	<insert id="save" parameterType="Survey" useGeneratedKeys="true" keyProperty="id" >
		insert into survey (user_id, paper_id, paper_url, subject, groups_id, sent_timestamp, deadline_tiemstamp, is_anonymous, description)
		values (#{creater.id},#{paper.id},#{paperURL},#{subject},#{surveyGroup},now(),#{deadlineTiemstamp},#{isAnonymous},#{description});
	</insert>
	
</mapper>